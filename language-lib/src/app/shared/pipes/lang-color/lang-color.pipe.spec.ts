import { LangColorPipe } from './lang-color.pipe';

describe('LangColorPipe', () => {
  it('create an instance', () => {
    const pipe = new LangColorPipe();
    expect(pipe).toBeTruthy();
  });
});
